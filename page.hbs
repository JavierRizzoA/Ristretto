{{!< default}}

<main class="content" role="main">
	<article class="{{post_class}}">
	{{#post}}
		<header>
			<h2>{{title}}</h2>
		</header>
		<section>
			{{content}}
		</section>
		<footer>
			<section>
				{{tags prefix="Tagged "}}
			</section>
		</footer>
	{{/post}}
	</article>
</main>
